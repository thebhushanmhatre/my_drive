- @docs.each do |doc|
  %h2= link_to doc.title, doc 
  %p= 'Created "' + time_ago_in_words(doc.created_at) + '" ago'
  %p= 'Last Updated "' + time_ago_in_words(doc.updated_at)  + '" ago'
  %p= truncate(doc.content, length:100)

= link_to "Create New Doc", new_doc_path